<!--
   Name: Kennedy Page
   Date: 12.07.24
   CSC 372-01

   This file defines the view for displaying detailed information about a specific customer order 
   in the admin section of the BookedUp project. It provides order details, such as customer information, 
   items purchased, quantities, total price, and order status.

-->

<h1 class="mb-4">Orders for <%= customer.username %></h1>
<table class="table">
    <thead>
        <tr>
            <th>Order ID</th>
            <th>Book</th>
            <th>Quantity</th>
            <th>Price</th>
            <th>Total</th>
            <th>Date</th>
        </tr>
    </thead>
    <tbody>
        <% orders.forEach(order => { %>
            <tr>
                <td><%= order.order_id %></td>
                <td><%= order.title %></td>
                <td><%= order.quantity %></td>
                <td>$<%= order.price.toFixed(2) %></td>
                <td>$<%= (order.quantity * order.price).toFixed(2) %></td>
                <td><%= new Date(order.created_at).toLocaleString() %></td>
            </tr>
        <% }) %>
    </tbody>
</table>

<div class="container mt-4 d-flex justify-content-center my-3">
  <!-- Back to Dashboard Button -->
  <div class="mb-3">
    <a href="/admin" class="btn btn-secondary">Back to Dashboard</a>
  </div>