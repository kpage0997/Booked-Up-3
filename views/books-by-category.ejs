<div class="container mt-4">
    <h1 class="text-center mb-4" style="font-weight: 500;">Books in <%= categoryName %></h1>
    <p class="text-center text-muted" style="font-size: 1.1rem;">
        Browse the best selection of <%= categoryName %> books.
    </p>
    <div class="row mt-4">
        <% books.forEach(book => { %>
            <div class="col-md-3 mb-4 mt-4 text-center">
                <img src="<%= book.image_url || '/images/default.jpg' %>" 
                     alt="<%= book.title %>" 
                     class="img-fluid rounded mb-2" 
                     style="height: 200px; object-fit: cover;">
                <h5><%= book.title %></h5>
                <p class="text-muted">By <%= book.author %></p>
                <a href="/books/<%= book.id %>" class="btn btn-primary btn-sm mt-2">View Details</a>
            </div>
        <% }) %>
    </div>
</div>